const classes = require('./classes.js');

describe('test suite for classes.js', () => {
    it('should test the dog class', () => {
        const testClass = new classes.dog('Jimmy', '38cm', 'black')
    })
})